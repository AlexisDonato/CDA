{{ form_start(selectForm) }}

<div class="row">
	<div class="form-group col-md-6">
        <h3 class="mt-3">Choisissez une adresse de FACTURATION :</h3>
		{{ form_row(selectForm.selectBillingAddress, {
            label: " "
        }) }}
	</div>
	<div class="form-group col-md-6">        
        <h3 class="mt-3">Choisissez une adresse de LIVRAISON :</h3>
		{{ form_row(selectForm.selectDeliveryAddress, {
            label: " "
        }) }}
	</div>
    </div>
</div>
<div class="text-center">
    <button class="btn btn-info border-light btn-md">{{ button_label|default('Enregistrer vos choix') }}</button>
</div>
{{ form_end(selectForm) }}
