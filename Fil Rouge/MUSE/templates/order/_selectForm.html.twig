{{ form_start(selectForm) }}
<div class="form-group mt-5">
	<h3 id="audioFont" class="mt-3 text-info">Choisissez une adresse de FACTURATION :</h3>
	{{ form_row(selectForm.selectBillingAddress, {
            label: " "
        }) }}
</div>
<div class="form-group mt-5">
	<h3 id="audioFont" class="mt-3 text-info">Choisissez une adresse de LIVRAISON :</h3>
	{{ form_row(selectForm.selectDeliveryAddress, {
            label: " "
        }) }}
</div>
<div class="form-group text-center mt-3">
	<button id="audioFont" class="btn btn-info border-light btn-md mt-3 productHl">{{ button_label|default('Enregistrez vos choix') }}</button>
</div>
{{ form_end(selectForm) }}
